const ElHeader = ({ MyNiceColor }) => (
  <header
    className="header-wrapper"
    style={{ backgroundColor: MyNiceColor, padding: 10, textAlign: "center" }}
  >
    mi awesome apps
  </header>
);

export default ElHeader;
